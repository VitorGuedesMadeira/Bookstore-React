/*
// create new app (Unique Identifier)
const newId = async () => {
  const answer = await fetch('https://us-central1-bookstore-api-e63c8.cloudfunctions.net/bookstoreApi/apps/', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
  }).then((data) => data.text());
  return answer;
};
*/
